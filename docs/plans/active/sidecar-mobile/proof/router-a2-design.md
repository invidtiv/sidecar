# Mobile M1-A2 owning-host router design

**Task:** `td-762c04`. **Baseline:** the implementation began from integrated main `f92968a89e59ad3e59ce2f6eecb7071345952499`. **Status:** the route authority, owner stream, public identity mapper, owner catalog router, protocol broker, directory/CLI wiring, explicit owner capability, cold-owner lookup, and host-list feature-state parity slices passed focused independent review and are integrated through `ba78d35cf836f87f772223f254acda5fae95da20`. The native routed UI journey, normal two-machine owner deployment, read-only fresh-process view, and same-config desktop Sessions parity proof also passed.

## Authority boundary

The hub routes a selected mobile protocol stream to `sidecar mobile serve --stdio` on the machine that owns the terminal. It starts that process through the registered host client's existing `SidecarCommand` SSH channel. The owner retains the durable managed-shell resolver, tmux capture and input transport, strict conditional geometry lease, live config-generation checks, presence timeout, history capture, and EOF cleanup. The hub does not construct a remote tmux manager, parse terminal frames, synthesize input or heartbeat, or fall back to a same-named local session.

The registry supplies one atomic route authority containing the current client pointer, process-local client incarnation, stable registration fingerprint, and current online mobile capability. The fingerprint covers the registered host ID, SSH target, remote binary, remote config, and ordered environment. A hub handle additionally binds the hub config generation and the exact owner `TargetIdentity` returned by the owner's mobile catalog or resolve path. Before starting the owner stream and before each forwarded target operation, the router requires the same registered client, runtime incarnation, fingerprint, online health, mobile capability, hub config generation, owner host identity, and target identity. Host removal, disablement, retargeting, client replacement, stale health, owner process restart, owner config change, server restart, pane reuse, or durable shell replacement refuses or closes the route.

The registry client incarnation prevents delayed work from an old client being accepted inside one hub process. The registration fingerprint is stable across hub processes and proves that a fresh hub still addresses the same configured owner. Neither value replaces terminal identity. A fresh owner mobile process re-resolves the client-supplied exact `TargetIdentity`; the owner remains the authority for its current config and terminal incarnation.

## Protocol and transport

The ordinary host `hello` has an additive `mobileOwnerServeV0` verb capability for the fixed `mobile serve --stdio --owner-only` command. It is separate from the older `mobileServeV0` capability, so a hub never passes the new flag to a host that only knows the recursive-capable command. The router still starts the actual owner stream and requires a valid mobile `hello` with the exact expected public protocol capabilities before forwarding a resolve or reconnect. A generic Sidecar version, a cached host snapshot, or a stale-permitting one-shot invocation is never treated as mobile authority.

The public native protocol remains unchanged. The hub's catalog composes its local owner catalog with current remote owner catalog snapshots and applies the shared A1 sorting, filtering, and grouping rules. Remote ready rows come from the owning mobile service, including the exact expected target identity; host observations alone remain descriptive. Unavailable, disabled, connecting, stale, incompatible, and failed owners stay explicit rows and never carry attach authority.

Once a resolve or reconnect selects an owner, the router binds random hub target and attachment handles to the corresponding owner handles. It translates only these opaque identifiers and hub correlation metadata. Request and response ordering, operation and output sequences, reset generations, geometry, modes, normalized VT, bounded history snapshots, errors, and retry facts pass through without reinterpretation. No owner handle is exposed where another owner stream could accept it.

The hub forwards only requests received from the phone. It never generates heartbeats to keep a lease alive. Phone EOF, owner EOF, SSH failure, registry removal or retargeting, stale-host detection, config mismatch, protocol failure, line overflow, or outbound backpressure cancels the owner process and closes its stdin. The owner's existing cleanup then releases only that attachment's exact lease. A headless registry tick calls `MarkStaleIfQuiet`; it does not manufacture host or terminal activity.

## Bounded implementation slices

The first review boundary adds the mobile capability plus the atomic registry route authority and validation API, with injected tests for unchanged, removed, retargeted, replaced, stale, and capability-missing clients. The second boundary starts and validates an owner mobile stream with bounded line and stderr handling, EOF cancellation, and no autonomous requests. The third composes authoritative owner catalogs and remaps handles across the full protocol, then proves a local owner and a separate loopback SSH owner with distinct configs, state trees, tmux sockets, Sidecar processes, and duplicate session/pane names. Every proof clears `TMUX` and `TMUX_PANE` and cleans only its owned resources.

The real private Aerie-to-MarcusBook route passed without changing either machine's global config, installed binary, default tmux server, or user state. The reviewed hub binary `f211b839180d9db5eae88982258f10ea3a7a227eec591d3e0bd0e19f693d94ea` selected one of two remote candidates, resolved and opened it, acquired control, delivered one synthetic input, accepted heartbeat, released its exact lease on phone EOF, and reconnected through a distinct hub API process at attachment generation 2 without replay. The lease was empty after EOF and after explicit release. The payload-free result and transcript were independently reviewed at SHA-256 `743095f3eea45fce617b0037cc7e30f11d1b31abb06f219a5706ca9dec120d97` and `f8fa23c8995eb42ca277f77e757ccca92f003c3a14189db107d0c9f18fb969a6`.

A separate disposable two-owner proof used identical local and remote `sidecar-sh-project-1` / `%0` terminal identities. Their public selectors remained distinct; remote input and accepted 70x20 geometry reached only the remote owner while the local pane stayed 80x24 and contained no remote marker. Thirty forwarded input/heartbeat operations during 8.3 Hz terminal output measured 27.161 ms median, 137.537 ms p95, and 177.258 ms maximum from each request write to its correlated acknowledgment, with 97 increasing post-resize frames and the exact newest synthetic line visible. These timings do not measure keystroke-to-visible-frame latency. A MobileServeV0-only owner stayed unsupported with no rows. Hub config retarget during control refused a stale release and left the owner empty; killing only the remote proof tmux server closed that route, refused its old identity, and left the same-named local terminal alive without fallback. The result, payload-free transcript, and checked-in driver hashes are `305e50bcdbcb93dd150569cbeaa59a935d2e1c980b49a8ddde523cb773c2f3f7`, `89f40b5b241ebfc122f0a600582cc40785202da28116dc8b487b38add91ec5f5`, and `28795583c9a4559082b14d2c22252bb3ec50d3cd5c3e5a3b884da96c008d6327`. The remote disposable server was stopped; the stable native route environment was not modified.

The signed native app then exercised the stable routed endpoint on loopback port 22230 against the exact MarcusBook candidate. It applied frames 1 through 18 without a gap or error, sent 12 input requests and five heartbeats, rendered `APP-OK`, read two authoritative history snapshots across a 46x19 to 46x38 resize, and released control. A fresh app catalog reused the route. The native evidence manifest, payload-free wire metadata, and review note are pinned at SHA-256 `7b5d75d845e8f1d9245f779d6afa199571a31ee13f17390235fb5ba747e02680`, `c1e73d7b43e4d076f0203ac7592d44962ae5d6012245fdb64d57bec30563f144`, and `74ea9b8b7ff04b38e72708050d783ea746f10232c36d83e3574667e630846e99`; the reviewed native source commit is `718c53d8ebdf75f1fe99fa403759a5a75ac7e16e`.

The normal registered MarcusBook owner was upgraded without moving its clean main checkout. Commit `ba78d35cf836f87f772223f254acda5fae95da20` was imported through a minimal bundle into a detached worktree, verified with pinned modules and the nine affected package tests, then activated through the managed installer. The installed arm64 artifact is SHA-256 `cdff90a0105f21f2d0ca1072d69869fc43981c49f291cecf1bacaac69b3e5867`; its ordinary host hello advertises both `mobileServeV0` and `mobileOwnerServeV0`, and its owner-only mobile hello reports the exact v0 capabilities. The previous 5ac artifact remains available at SHA-256 `b987609edce6569b5694bc171480de47e96bc64058c39f06a174afe917b6a97a`. A cold normal hub process first returned 64 local rows while the remote host was explicitly `connecting`; a native-budget refresh reached two online hosts, 92 bounded rows, 17 ready rows, one ready MarcusBook row, and no collection failures. A separate fresh terminal process then completed hello in 0.174 seconds, exact remote resolve in 2.980 seconds, open in 0.954 seconds, and delivered its initial frame in view-only mode. That proof sent only hello, resolve, and open, discarded the terminal payload, left control false, and preserved source geometry. Its result is SHA-256 `4d657effd0e082ed0439ad12c5023f4c2d7810b3754380a2db15b6e9eaedaecd`; the same-process catalog cadence artifact is `4a5534825fccce422fd7064923567d70c74fe849db180515fe26e2a0019107a5`. No global feature, config, source checkout, or tmux server changed.

## Reproduction and surface parity

`scripts/mobile-router-proof.py LOCAL_ROOT SSH_ALIAS REMOTE_ROOT` is the repository-native adversarial driver. Both roots must be absolute task-owned `/private/tmp/sidecar-mobile-router-*` directories containing `.sidecar-mobile-router-proof-owner` with the exact value `sidecar-mobile-router-adversarial-proof-v0`. The local root supplies `bin/sidecar`, `config/hub.json`, isolated state and tmux trees, a local `sidecar-sh-project-1` / `%0`, and fixed remote and legacy owner registrations. The remote root supplies its matching isolated wrapper, config, state and private tmux server with the same session and pane names. The driver clears inherited tmux coordinates, refuses unmarked roots, reads each owner catalog directly, and proves both public expected targets derive from the marked private socket, tmux server/session/pane, and durable shell identity before any control or input. Every request has one total deadline even while frames arrive. SSH or owner-option reads fail closed. Before the destructive owner-loss step and cleanup, the driver rechecks the marked remote socket inode, server PID, and session creation identity. It restores the hub config, closes every service process, destroys only the revalidated remote proof server, and leaves the local server alive for the final no-fallback check. It writes a payload-free transcript and result into the local root.

A separate task-owned desktop fixture ran the real Sessions surface against the same two-owner configuration as `mobile sessions --sort activity`. Desktop showed two live-shell rows: the local `Twin` first and the host-labeled `remote-desktop · Twin` second, exactly matching the API live-shell subset order and host identities. The unfiltered API snapshot also contained two no-session worktree rows that were not displayed in this desktop screenshot, so this evidence does not claim all-states row-universe parity. The selected preview reported no captured output and remained read-only in this fixture: both exact private terminals kept empty owner tokens and 80x24 geometry before, during, and after the viewer. The viewer and both marked private servers were then stopped. The result is SHA-256 `6acd8f17c032da470f05a2dfd6a9a0324e002619f9e305951c14202769742e7c`; the screen and matching CLI catalog are pinned inside it. This proves actual desktop/API catalog parity without claiming a list-only mode or a control-taking preview.
