# Sidecar mobile: M0 and M1 execution

**Controlling plan:** [Sidecar mobile](../sidecar-mobile.md). **Status:** readiness and mockups approved; M0-A/B, M0-C and M0-D independently approved. The Go service is committed at `cf77be7a` (reviewed producer `712aeb2d`), and the native client at `../sidecar-mobile` commit `4335ff9`. The native simulator has proved exact input across keyboard resizing, background/fresh-process view-only reconnect and desktop/mobile conversation handoff. Physical M0-E is underway: the iPhone has connected, typed into the existing agent, rotated and handed control back to desktop. A physical connection has also resumed through a fresh SSH process. A normal-output capture-coalescing defect was observed and is being repaired; iPad checks remain open. M1-A1 local catalog child `td-8e1dc1` is independently approved and integrated at `4ab09264`; the cross-host M1-A parent remains open for A2. M0 is not complete. **Epic:** td-5d82c2. **Source baseline:** Sidecar `352c7eb7`, Jumar `ae72637bb88736a71f8ce4822ebe7d7d31f83b5d`, inspected 2026-09-07. Recheck the exact source revisions and active ownership at dispatch.

## Start here

The first useful journey is: open the universal app → connect to `aerie` over authenticated SSH → select one known managed shell → view its current terminal → deliberately enter control → type into the existing agent → leave or reconnect without replaying input or fighting desktop geometry. iPhone and iPad share this behavior; their navigation adapts to available width.

The first engineering step is **M0-A/B, a terminal seed producer and consumer spike**. It may start without Apple signing, a device, App Store Connect credentials, a push relay, or importing Jumar code. It must use synthetic fixtures and isolated tmux/state. Production terminal extraction waits for the spike decision and independent review. Mockups and toolchain readiness run beside the spike. A directory scaffold, browser terminal simulation, or passing JSON decoder alone is not M0.

Marcus has selected `aerie` as hub, Apple team `<APPLE_TEAM_ID>`, iOS/iPadOS 26.0 deployment support, and native Liquid Glass with the actual `sidecar-modern` colors. Use the public 26.5 simulator baseline and do not require a beta SDK. His connected iPhone 17 Pro and iPad Pro 12.9-inch (5th generation) run 27 beta. Local M0 proof may use those devices with the beta status recorded; public-supported-OS hardware evidence is required before distributed beta, not before M0/M1 work. Both devices are paired with Developer Mode enabled. An earlier signed candidate installed and launched on both. The independently reviewed native build `4335ff9` is installed and has now launched on both after unlock. The iPhone has completed authenticated live input and rotation; physical iPad proof and the observed capture-coalescing repair remain M0-E gates. Agent work is explicitly authorized; the coordinator assigns bounded ownership and integrates reviewable results.

## Repository and contract boundaries

| Location | Owns | Does not own |
| --- | --- | --- |
| `sidecar` | This controlling plan; Go headless service; target, ownership, ordering and host rules; mobile CLI; canonical protocol/fixture corpus; isolated hub demo | SwiftUI, Keychain, platform permissions, App Store metadata |
| `../sidecar-mobile` | Universal Xcode app; `SidecarClient`; SSH exec/terminal adapters; Swift fixtures consumer; mockups, native previews, readiness and device evidence; app distribution tooling | A second target resolver, lifecycle classifier, geometry policy, or catalog sort definition |
| `../jumar` | Read-only upstream baseline | Sidecar-specific changes or a second authoritative mobile plan |

Proposed production packages are `internal/mobile` for the service and `internal/mobileproto` for the wire envelope. Final names can follow the first extraction; do not add a generic remote-control framework. The API runs as `sidecar mobile serve --stdio` with no PTY. Catalog/status queries get documented JSON CLI paths. Terminal control is already a documented non-interactive API path; duplicating it into a second low-level prompt system is unnecessary.

The Go service owner owns `testdata/mobile-protocol/v0/` in Sidecar. The initial spike owns its `experimental-terminal/` child until review transfers it. The Swift owner consumes a vendored read-only snapshot under the mobile client's test fixtures with a manifest recording the Sidecar commit and SHA-256 values, plus one explicit sync command. No symlinks into a sibling checkout and no separate hand-maintained golden corpus. The fixture owner changes both producer expectations and the manifest; the consumer proves decoding and resulting emulator behavior against that pinned revision. Make the spike namespace visibly experimental and do not advertise wire compatibility until its shape is settled.

The production envelope should specify protocol version, request/attachment IDs, output sequence, reset boundary, accepted operation acknowledgements, capability/refusal codes, and finite message/buffer bounds. The exact constants and seed representation are selected and measured in the spike. Protocol mismatch fails before opening a terminal. Do not send secrets, real terminal transcripts, or private host credentials into fixtures.

## Source seams and extraction constraints

| Current source evidence | Reuse and required work |
| --- | --- |
| [`workspaceinventory.Collector.ValidateWorkspace`](../../../../internal/workspaceinventory/inventory.go) revalidates durable identity; `Pane` carries server PID, and shell manifests carry `CreatedAt` | Reuse collection/revalidation. An inventory row or `%pane` alone does not authorize input; bind an opened handle to owner host, config/client incarnation, tmux server incarnation, durable session identity, pane, and attachment generation. Revalidate on foreground/reconnect and before mutation. |
| [`hosts.Registry`](../../../../internal/hosts/registry.go) already exposes `Incarnation`, `Updates`, `Client`, `RunSidecar`, `RunTmux`, and `ProjectResults`; [`Client.ControlCommand`](../../../../internal/hosts/client.go) returns a remote control channel command | Compose a headless catalog from these seams. Keep current remote failure and stale-client fencing; never construct an overview/Bubble Tea model just to obtain data. Hub restarts need a new API instance token as well as registry incarnation. |
| [`workspacelist.Sorted`, `Filtered`, `GroupedAt`](../../../../internal/workspacelist/list.go) contain shared pure sorting/grouping; Activity ranks groups and recency, Project preserves configured project order, Recent uses descending `ChangedAt` | Call or narrowly extract these rules. Do not translate a guessed alphabetical Project ordering or activity rank into Swift. The API should return ordered/grouped results for requested sort/filter/search; phone view preferences remain local. |
| [`hostproto.Version`](../../../../internal/hostproto/hostproto.go) is 2, and its stream remains host-to-viewer; `KindUIRequest` is a forwarded display request, not a bidirectional terminal RPC | Preserve it. [`hosts/run.go`](../../../../internal/hosts/run.go) invokes owned remote CLI verbs over the existing SSH master; use the owning host's established paths. Mobile gets a separate versioned stdio API. |
| [`ControlRequest`](../../../../internal/tty/control_manager.go) exposes snapshots, model frames and invalidations, not a public ordered raw-seed/output feed | Reuse its actor and seed transaction after the spike. `OnSnapshot`/`OnModelFrame` must not block the control reader; put bounded handoff outside callbacks and reset explicitly on overflow. |
| [`buildSeedCommands` and `seedMetadataFormat`](../../../../internal/tty/control_model.go) capture metadata, saved main grid and active grid in one ordered transaction; metadata includes 15 fields and two mouse tracking modes are explicitly unavailable | This is valuable ordering machinery, not an arbitrary-emulator checkpoint. Current rendition, scroll margins, wrap/parser state and all input modes are not fully exported. Audit them rather than assuming a capture string can resume an independent SwiftTerm correctly. |
| [`screenmodel.Seed` and `Frame`](../../../../internal/tty/screenmodel/model.go) preserve active/main captures, cursor, geometry, history and mouse state; frame adds model-observed bracketed paste/cursor style | `Frame.Output` is presentation output, not the raw PTY stream. State learned only after attaching cannot establish what was active before attaching. Treat the Go model as one consumer and verify the Swift emulator independently. |
| [`remoteTerminalBackend`](../../../../internal/tty/control_remote.go) uses in-band input, geometry, bounded capture and explicit no-local-fallback behavior, but holds a `*tty.Model` and uses model lifecycle queues | Extract the smallest headless backend/session seam only after the seed decision. Preserve ordered input, delayed-release fencing and remote refusal rules, and make desktop reuse the extracted decisions. One agent owns production edits across `tty` for this slice. |
| [`DecideGeometryLease`](../../../../internal/tty/geometry_lease.go) is already state-free; the keeper uses host/PID identity and process liveness | Reuse the policy unchanged unless a reviewed mobile requirement forces a change. Choose an attachment identity that cannot alias another phone attachment; a hub PID staying alive cannot count as mobile presence. Expire mobile evidence on lost heartbeats and stop refresh on background/disconnect. Default foreign-owner stale detection can take at least 60 seconds; measure and report the actual takeover path rather than promising immediate expiry. |
| Jumar `JumarTransport/TerminalTransport.swift`, `JumarSSH/SSHConnection.swift`, `SSHShellTransport.swift`, and `TerminalScreenModel.swift` at the pinned revision | `SSHConnection.openSessionChannel` is reusable; `SSHShellTransport.start` always requests a PTY and uses an unbounded event stream, so it is not the mobile API transport. Add a no-PTY exec channel adapter. Sidecar's model must honor remote accepted resize, bound queues and revalidate reconnect; Jumar's current screen ignores `remoteResize` and only degrades on background. |

Jumar pins SwiftTerm 1.18.0 (`7691f85b222a67a66b58499e1b2647443cf0dda7`), NIO SSH 0.15.0, NIO 2.101.3, and NIO Transport Services 1.28.0 in its app lockfile. Preserve those observed pins for reproduction; a package manifest's open lower bound does not replace a resolved lock. The app declares iOS 26.0 and families `1,2`, while README/CLAUDE say 26.5 and JumarCore says iOS 18/macOS 15. Compile the reused components for the selected 26.0 deployment target and audit newer APIs for availability guards or fallbacks. A 26.5 simulator run alone does not establish the 26.0 API boundary. Do not combine the steel thread with speculative library upgrades.

Jumar has no root license file at this inspected revision. The existing plan records the upstream author's permission reported by Marcus. Local exploration may proceed under that permission; any import needs its exact revision, file paths, attribution and local delta recorded. Before sharing a build or publishing imported source, record concrete reuse/distribution terms. Do not copy unrelated `JumarHerdr`, voice or notification-policy modules merely because they are adjacent to the terminal code.

## Terminal-seed decision gate

**Reviewed result (2026-09-07):** td-415417 and td-322041 passed independent cross-review. Capture seed plus raw continuation was falsified by hidden rendition and wrap state. Proceed with ordered tmux-authoritative normalized presentation frames and explicit input modes. The pinned SwiftTerm consumer passes 11 focused tests, including Unicode, colored blanks, mode handling, reply discard, and sequence-gap reseeding. The [producer proof](proof/seed-spike.md) and mobile `Spikes/TerminalSeed/REVIEW.md` record the bounded evidence and limits. This approval permits the M0-C extraction; real transport, capture cost, supported-tmux capability probes, attachment ownership, and physical-device terminal proof remain required.

The Go spike owner starts in a fresh isolated Sidecar worktree and runs `make worktree-init` first. Own only `internal/tty/mobile_seed_spike_test.go`, `testdata/mobile-protocol/v0/experimental-terminal/**`, and `docs/plans/active/sidecar-mobile/proof/seed-spike.md`. Package-local tests can inspect existing private seed methods without adding a public production API. The Swift spike owner owns `../sidecar-mobile/Spikes/TerminalSeed/**`, consumes those fixtures, and uses the pinned SwiftTerm emulator without importing Jumar application code. This division permits early harness setup while the producer finishes fixtures.

Evaluate these alternatives in order; record the selected strategy, rejected alternative and observed cost in spike evidence:

1. **Explicit snapshot plus ordered VT output:** emit a reset/seed that restores the required current terminal state, then exact post-seed bytes. Prove main/alternate screen restoration, cursor, rendition, scrolling and input modes using SwiftTerm. Every state that cannot be reconstructed is an explicit support limit, not a default inferred from a blank emulator.
2. **Server-normalized frames with explicit input state:** if exact raw continuation is unsound, use canonical full/delta screen presentation from the Go owner and an explicit input/geometry contract. Prove that the chosen SwiftTerm rendering adapter does not reinterpret normalized frames as a second independent application stream; measure bandwidth, selection, history and cursor behavior. The result must still be a live interactive terminal, not text polling passed off as complete fidelity.
3. If neither meets the required agent journey, stop production extraction and report the specific failing state and smallest next experiment. A normal PTY `tmux attach`, unbounded transcript replay, or dropping unrecognized sequences silently does not satisfy this gate.

The required corpus is small but adversarial: a colored shell with trailing colored blanks; a live agent TUI in alternate screen; exit back to the saved main screen; cursor movement and hidden cursor; Unicode/combining/wide cells; wrap and scroll boundaries; bracketed multiline paste; application cursor/keypad behavior; a terminal query with exactly one reply owner; output racing the seed; a simulated sequence gap/overflow; resize and reconnect with queued input. Include a terminal whose rendition/modes were set before attachment, since a fixture that emits all modes after attach hides the hard problem. Record any supported-agent limitations before deciding whether the gate is sufficient.

The geometry portion tests two distinct attachments in one hub process, background/disconnect evidence expiry, stale release after a new owner, lease-read failure, wrong server incarnation, and changed target generation. It must establish how mobile identity fits the host/PID contract and host-side `sidecar open`/layout refusal. Avoid changing the shared lease token format unless the audited existing contract cannot represent safe attachment identity.

**Pass:** the Go producer and SwiftTerm consumer agree on the required screen/input state before and after continuation; stream discontinuities reset instead of silently diverging; input is never replayed; the selected lease identity has a credible live/disconnect path; independent review accepts the proposed production shape. Record unsupported modes and measured overhead. One emulator accepting a fixture is insufficient.

## Dependency-ordered ownership

All tasks below are under td-5d82c2. They are open until a coordinator assigns and starts them. td dependency edges mean completion prerequisites; bounded preparation may overlap where stated, without claiming the dependent outcome.

| Slice and task | Sole implementation owner | Dependency and acceptance handoff |
| --- | --- | --- |
| Readiness `td-02601e` and screens `td-e4f130` | Tooling owner: mobile readiness docs/tool setup. Design owner: mobile repo/mockups. | Independent of seed work. Verify Xcode/runtime and interactive compact/regular designs; report device/team input needed for signing. |
| M0-A `td-415417` | Go seed spike paths above, no production source extraction | After plan review. Hand synthetic fixtures, seed gaps, lease alternatives and isolated proof to M0-B. |
| M0-B `td-322041` | Mobile `Spikes/TerminalSeed/**` and consumer evidence | Harness setup can overlap M0-A. Complete only against its reviewed fixtures and final strategy decision. |
| M0-C `td-bce41c` | Sidecar `internal/mobile`, `internal/mobileproto`, production `tty` extraction, mobile CLI wiring/docs, canonical fixtures | After M0-A/B pass. Prove one local target through scriptable JSONL: open, control, input, output, accepted resize, release and reconnect. Existing desktop shared-core checks must pass. |
| M0-D `td-0e2484` | Mobile native application, Sidecar client and SSH/terminal adapters, fixture synchronization | After M0-C contract is usable, readiness and mockup review. Native shell setup may overlap with agreed fixtures. Prove compact iPhone and regular/narrow iPad layouts on a real isolated service. |
| M0-E `td-2b2c65` | Independent proof owner: evidence and a mobile-connectable isolated demo helper; implementation fixes go back to their owners | After M0-C/D. Physical iPhone mandatory; connected iPad too, with any missing iPad run recorded. Both personal devices are 27 beta; combine their local proof with the public 26.5 simulator and defer public-supported-OS hardware evidence only until distribution. Review and prove reconnect, input, actual keyboard geometry and desktop/mobile control handoff. |
| M1-A `td-ffe174` | Sidecar catalog/query service, canonical catalog fixtures and narrowly required shared inventory/list/host extraction | After M0-C stabilizes; can overlap mobile device work. Compose local/remote rows with shared sorting, stale/capability states and exact identities. Preserve active remote-plan ownership. |
| M1-B `td-a56f0d` | Mobile Sessions UI, candidate picker, compact/regular state, client integration and proof evidence | After M1-A and M0-E. Prove native local/remote browse-to-terminal and same-config desktop parity. Independent reviewer must exercise collision, outage and retargeting cases. |

A reviewer must not be the implementer of the slice being approved. Use its own `TD_CONTEXT_ID`; record actual review provenance with `td handoff`, `td review`, and `td approve`. Keep focused commits per repository and stage only the owner's changes. No push, TestFlight upload, App Store submission, or permanent global feature/config change follows implicitly from local proof.

## Readiness gates by first use

| Required for | Concrete prerequisite | Current state / action |
| --- | --- | --- |
| Browser mockups and Go seed spike | Local repo, Go, isolated worktree/socket/state, synthetic fixtures | Ready to prepare now. Xcode, signing, APNs and ASC credentials are independent. |
| Swift emulator and simulator | Completed public Xcode, selected developer directory, 26.5 simulator runtime and any renderer toolchain component | Marcus reports Xcode updated; tooling owner verifies actual versions and focused build for deployment target 26.0. A README's requirement is not proof a new component download is necessary. |
| Native local network connection | `aerie` reachable, SSH service/account, verified host key, app Local Network purpose string and device permission | Inspect first, then configure only the concrete missing authorized prerequisite. Never disable host-key verification. |
| Physical M0 | Team selection, unique development bundle identity, signing, trusted device with Developer Mode; controlled hub credentials | Team `<APPLE_TEAM_ID>`, bundle `com.haplab.sidecar`; iPhone 17 Pro and iPad Pro 12.9-inch (5th generation) are paired with Developer Mode enabled on 27 beta. Automatic development signing and installation of reviewed native build `4335ff9` succeeded on both. Both launched successfully after unlock with an isolated proof profile supplied only to each process. The iPhone has completed authenticated live input and rotation; fresh-process reconnect is also observed; remaining iPad checks and the capture-coalescing repair keep M0-E open. Do not collect private keys in chat. |
| M1 remote proof | Registered remote host capability, reachable SSH route, compatible Sidecar and private isolation on both machines | Root's live `sidecar host list --json` found registered/enabled `marcusbook` but `featureEnabled:false`. Verify host and use a temporary isolated config enabling remote hosts for proof; do not flip the user's global flag as readiness work. |
| Distribution/TestFlight | App Store Connect authentication/app identity and signing; public-supported-OS physical-device proof; explicit Jumar terms and attribution; export/privacy/metadata decisions | ASC CLI installation is authorized preparation. Account/team/app mutations and publication require their concrete destination to be established. These do not block M0/M1 local builds. |
| M3 background alerts | Relay/operator decision, awake resident observer, APNs entitlement/provider credentials and enrolled device | Deferred to M3. Do not request notification permission or install a resident service during M0 merely to finish a checklist. |

Marcus subsequently authorized continued M1 implementation while away. The real iPhone attachment, exact input and geometry handoff, plus a physical fresh-process reconnect, now establish the production shape; bounded native Sessions work may overlap the remaining iPad and coalescing-repair proof without declaring M0-E complete. His requested keyboard dismissal without releasing control and terminal scrolling are explicit next-build work: native interaction task `td-89334f` and bounded authoritative history task `td-595a28`. History must come from the owning terminal and cannot be synthesized by accumulating normalized full-screen frames.

App Store Connect CLI 4.11.0 authentication now succeeds using an existing System Keychain credential. Filtered Sidecar app/bundle-ID and distribution-certificate queries return no accessible records. A read-only query of an existing app's bundle ID returns App ID prefix `<APPLE_TEAM_ID>`, matching the selected development team. Sidecar's development profile uses a wildcard identity, explaining why development installation did not create a dedicated bundle-ID record. Distribution preparation is tracked in the mobile repository's `docs/readiness/testflight-readiness.md`; no upload or external account mutation follows from this read-only audit.

For M0, reuse Jumar's password authentication only for a controlled local proof if necessary. Keychain-backed app key enrollment and practical revocation must be complete before a broader beta. The hub uses its existing credentials to reach configured remote hosts; do not export those credentials to the app.

## Minimum credible verification

The spike needs focused tests and one isolated real tmux transcript, then cross-emulator consumer evidence. Production M0 needs focused Go identity/lease/stream contracts, Swift decoding/terminal tests from the same fixture revision, a real stdio/SSH client journey, simulator compact/regular UI proof, physical iPhone interaction, and independent review. M1 adds the existing inventory/list/hosts checks affected by extraction and one isolated two-host journey; do not multiply every core test across every UI surface.

Inspect `scripts/tmux-drive.sh paths` or the mobile demo's equivalent before launch. Every proof must use a distinct socket, temporary Sidecar state, explicit temporary config, and `SIDECAR_ISOLATED_STATE=1`; clear inherited `TMUX`/`TMUX_PANE` or address the private socket on every command, including cleanup. Remote proof needs the same two-axis isolation on the owning host. Mobile access to the demo must be deliberately exposed through the validated SSH entry point, and cleanup may close only resources that proof created. Never restart or replace the default tmux server.

Evidence should name exact Sidecar/mobile/Jumar revisions, fixture manifest, simulator/device model and OS, host and private paths, the checked command/journey, expected and observed outcome, screenshots where relevant, and connect-to-usable-screen, keystroke-to-frame and throughput measurements. M1 also measures the remote extra hop. Do not retain passwords, private keys, real terminal transcripts or other secrets. Update milestone status only after the corresponding evidence exists.
