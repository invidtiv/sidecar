# Pi evidence and proof record

- Availability: `pi` does not resolve in Sidecar's 2026-08-08 login-shell PATH. Pi 0.67.68 exists only in an inactive mise Node 22.22.0 tree; no live process/title metadata was available.
- Authority: Herdr Pi manifest 2026.06.10.1, compatibility rule `Working...`; unmatched positively identified `pi` falls back to debounced idle. No lifecycle integration is installed.
- Explicitly unavailable: real startup, idle, working, tool, blocker, interruption, completion, viewer/settings, exit, text capture, and PNG. Pi exposes no manifest blocker rule.
- False-positive boundary: a returned shell is unknown and old manifest-backed working text outside the current-bottom window cannot win. Viewer/interruption vocabulary is unavailable and therefore unrecognized.
- Isolated proof: `tmux-drive.sh paths` resolved socket, state, cache, and config under `/tmp/sidecar-drive-501`; compatibility observations traverse detector and shared worktree/shell/List/Kanban tests. No real-provider artifact is claimed.
